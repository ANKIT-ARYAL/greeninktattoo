import React from 'react'; import { prisma } from '@/lib/prisma'; import { notFound } from 'next/navigation'; import Link from 'next/link'; import {  ChevronLeft,  Calendar,  Clock,  Phone,  MessageSquare,  Instagram,  ExternalLink, } from 'lucide-react';  export default async function BookingDetailsPage({ params }: { params: { id: string } }) { 
     // Await the params  
     const {id} = await params;  // Fetch the specific booking  
     // 
     const booking = await prisma.booking.findUnique({  where: { id: id }  });   if (!booking) {  notFound();  }  
      // Format the date  
      
      const dateStr = new Date(booking.scheduledAt).toLocaleDateString('en-US', {  weekday: 'long',  year: 'numeric',  month: 'long',  day: 'numeric',  });   const timeStr = new Date(booking.scheduledAt).toLocaleTimeString('en-US', {  hour: '2-digit',  minute: '2-digit',  });   return (  <div className="min-h-screen bg-black text-white p-6 md:p-12 lg:p-20">  <div className="max-w-4xl mx-auto">   {/* BACK BUTTON */}  <Link  href="/admin"  className="inline-flex items-center gap-2 text-neutral-500 hover:text-white transition-colors mb-12 group"  >  <ChevronLeft size={20} className="group-hover:-translate-x-1 transition-transform" />  <span className="text-[10px] font-black uppercase tracking-[0.3em]">Back to Dashboard</span>  </Link>   <div className="grid grid-cols-1 lg:grid-cols-3 gap-12">   {/* LEFT COLUMN: MAIN INFO */}  <div className="lg:col-span-2 space-y-12">  <header>  <div className="flex items-center gap-3 mb-4">  <span className={`px-3 py-1 rounded-full text-[9px] font-black uppercase tracking-widest ${  booking.status === 'PENDING' ? 'bg-orange-500/10 text-orange-500' : 'bg-emerald-500/10 text-white'  }`}>  {booking.status} Inquiry  </span>  <span className="text-neutral-600 text-[10px] uppercase tracking-widest">  ID: {booking.id.slice(-6)}  </span>  </div>  <h1 className="text-5xl md:text-7xl font-display uppercase tracking-widerer leading-none">  {booking.name}  </h1>  </header>   <section className="space-y-6">  <h3 className="flex items-center gap-2 text-white text-[10px] font-black uppercase tracking-[0.4em]">  <MessageSquare size={14} /> The Vision  </h3>  <div className="bg-neutral-900/50 border border-white/5 p-8 rounded-[2.5rem]">  <p className="text-neutral-300 text-lg leading-relaxed ">  {booking.description || 'No description provided for this project.'}  </p>  </div>  </section>  </div>   {/* RIGHT COLUMN: DETAILS & ACTIONS */}  <div className="space-y-6">  <div className="bg-neutral-900 border border-white/10 p-8 rounded-[2.5rem] space-y-8">   <div className="space-y-6">  <div className="flex items-start gap-4">  <Calendar className="text-white mt-1" size={18} />  <div>  <p className="text-[9px] font-black text-neutral-500 uppercase tracking-widest">Date Requested</p>  <p className="text-sm ">{dateStr}</p>  </div>  </div>   <div className="flex items-start gap-4">  <Clock className="text-white mt-1" size={18} />  <div>  <p className="text-[9px] font-black text-neutral-500 uppercase tracking-widest">Preferred Time</p>  <p className="text-sm ">{timeStr}</p>  </div>  </div>   <div className="flex items-start gap-4">  <Phone className="text-white mt-1" size={18} />  <div>  <p className="text-[9px] font-black text-neutral-500 uppercase tracking-widest">Contact Number</p>  <p className="text-sm ">{booking.contactNumber}</p>  </div>  </div>  </div>   <div className="pt-8 border-t border-white/5 space-y-4">  {/* WHATSAPP ACTION */}  <a  href={`https://wa.me/${booking.contactNumber.replace(/\D/g, '')}`}  target="_blank"  className="flex items-center justify-center gap-3 w-full bg-white text-black py-5 rounded-2xl font-black uppercase text-[10px] tracking-widest hover:bg-emerald-500 transition-all active:scale-95"  >  Message on WhatsApp <ExternalLink size={14} />  </a>   <button className="w-full bg-neutral-800 text-white py-5 rounded-2xl font-black uppercase text-[10px] tracking-widest hover:bg-red-500 transition-all">  Archive Inquiry  </button>  </div>  </div>   {/* QUICK PREVIEW IF THEY ATTACHED IMAGES (Placeholder logic) */}  <div className="bg-emerald-500/5 border border-emerald-500/10 p-6 rounded-[2rem] flex items-center gap-4">  <div className="p-3 bg-emerald-500/10 rounded-xl text-white">  <Instagram size={20} />  </div>  <p className="text-[10px] text-neutral-400 leading-tight uppercase tracking-wider">  Check client profile for style reference  </p>  </div>  </div>   </div>  </div>  </div>  ); }