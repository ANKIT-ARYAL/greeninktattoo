'use client'; import React, { useState, useEffect } from 'react'; import Image from 'next/image'; import { Plus, Trash2, Loader2, Edit3, Calendar } from 'lucide-react'; import AddBlogModal from '@/app/components/AddBlogModal'; import EditBlogModal from '@/app/components/EditBlogModal';  interface Blog {  id: string;  title: string;  excerpt: string;  content: string;  image: string;  createdAt: string; }  export default function BlogsAdminPage() {  const [blogs, setBlogs] = useState<Blog[]>([]);  const [loading, setLoading] = useState(true);  const [isAddModalOpen, setIsAddModalOpen] = useState(false);  const [blogToEdit, setBlogToEdit] = useState<Blog | null>(null);   const fetchBlogs = async () => {  try {  const res = await fetch('/api/blogs');  const data = await res.json();  if (Array.isArray(data)) setBlogs(data);  } catch (err) {  console.error("Fetch error:", err);  } finally {  setLoading(false);  }  };   useEffect(() => { fetchBlogs(); }, []);   const handleDelete = async (id: string) => {  if (!confirm("Are you sure you want to delete this blog post?")) return;  const res = await fetch(`/api/blogs/${id}`, { method: 'DELETE' });  if (res.ok) setBlogs(prev => prev.filter(b => b.id !== id));  };   return (  <div className="min-h-screen bg-black p-8 lg:p-12 space-y-10">  <header className="flex justify-between items-end max-w-7xl mx-auto w-full">  <div>  <p className="text-[10px] font-black uppercase tracking-[0.4em] text-white mb-2">Editorial Control</p>  <h1 className="text-4xl md:text-5xl font-display text-white uppercase tracking-widerer">Blog Manager</h1>  </div>  <button  onClick={() => setIsAddModalOpen(true)}  className="flex items-center gap-2 bg-white text-black px-8 py-4 rounded-2xl font-black uppercase text-[10px] tracking-widest hover:bg-emerald-500 transition-all shadow-xl active:scale-95"  >  <Plus size={18} /> New Article  </button>  </header>   {loading ? (  <div className="flex flex-col items-center justify-center py-40">  <Loader2 className="animate-spin text-white mb-4" size={40} />  <p className="text-neutral-500 text-[10px] font-black uppercase tracking-widest">Accessing Archives...</p>  </div>  ) : (  <div className="grid grid-cols-1 md:grid-cols-2 gap-8 max-w-7xl mx-auto">  {blogs.map((blog) => (  <div key={blog.id} className="group bg-neutral-900 border border-white/5 rounded-[2.5rem] overflow-hidden hover:border-white/20 transition-all duration-500 flex flex-col md:flex-row">  <div className="md:w-48 aspect-square md:aspect-auto overflow-hidden bg-black relative">  <Image  src={blog.image || "https://placehold.co/400x400/171717/white?text=Blog"}  alt={blog.title}  fill  sizes="(max-width: 768px) 100vw, 400px"  className="object-cover opacity-60 group-hover:opacity-100 transition-all duration-700"  unoptimized  />  </div>   <div className="p-8 flex-1 flex flex-col justify-between">  <div>  <div className="flex items-center gap-2 mb-3">  <Calendar size={12} className="text-white" />  <span className="text-[9px] text-neutral-500 font-black uppercase tracking-widest">  {new Date(blog.createdAt).toLocaleDateString()}  </span>  </div>  <h3 className="text-white text-2xl uppercase tracking-wider line-clamp-1 mb-2">{blog.title}</h3>  <p className="text-neutral-500 text-xs line-clamp-2 leading-relaxed">{blog.excerpt}</p>  </div>   <div className="flex justify-end gap-2 mt-6">  <button  onClick={() => setBlogToEdit(blog)}  className="p-3 bg-white/5 text-neutral-400 hover:text-white hover:bg-white/10 rounded-xl transition-all"  >  <Edit3 size={18} />  </button>  <button  onClick={() => handleDelete(blog.id)}  className="p-3 bg-white/5 text-neutral-400 hover:text-red-500 hover:bg-white/10 rounded-xl transition-all"  >  <Trash2 size={18} />  </button>  </div>  </div>  </div>  ))}  </div>  )}   {/* MODALS */}  <AddBlogModal  isOpen={isAddModalOpen}  onClose={() => setIsAddModalOpen(false)}  onSuccess={fetchBlogs}  />   {blogToEdit && (  <EditBlogModal  blog={blogToEdit}  isOpen={!!blogToEdit}  onClose={() => setBlogToEdit(null)}  onSuccess={fetchBlogs}  />  )}  </div>  ); }   