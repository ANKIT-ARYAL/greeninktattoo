'use client'; import React, { useState, useEffect } from 'react'; import Link from 'next/link'; import {  ImageIcon,  PenTool,  Calendar,  ChevronRight,  Clock,  AlertCircle,  Phone, } from 'lucide-react'; import { BookingRequest } from '../../types';  export default function AdminDashboard() {  const [bookings, setBookings] = useState<BookingRequest[]>([]);  const [loading, setLoading] = useState(true);   useEffect(() => {  const fetchBookings = async () => {  try {  const res = await fetch('/api/bookings');  const data = await res.json();  if (res.ok && Array.isArray(data)) {  setBookings(data);  } else {  setBookings([]);  }  } catch {  setBookings([]);  } finally {  setLoading(false);  }  };  fetchBookings();  }, []);   return (  <div className="min-h-screen bg-black flex text-neutral-400">  <main className="flex-1 p-12">  <header className="flex justify-between items-end mb-12">  <div>  <p className="text-xl uppercase tracking-wider mb-2">Control Center</p>  <h2 className="text-4xl font-display text-white tracking-wider uppercase ">Overview</h2>  </div>   <div className="bg-neutral-900 border border-white/5 px-6 py-4 rounded-2xl">  <p className="text-[10px] font-black text-neutral-500 uppercase tracking-widest mb-1">Total Inquiries</p>  <p className="text-3xl font-display text-white tracking-wider">  {bookings.length}  </p>  </div>  </header>   {/* Quick Actions Grid */}  <div className="grid grid-cols-1 md:grid-cols-2 gap-6 mb-12">  <Link href="/admin/designs" className="bg-neutral-900 border border-white/5 p-8 rounded-[2.5rem] group hover:border-emerald-500/50 transition-all duration-500">  <div className="flex justify-between items-start mb-4">  <div className="p-3 bg-white/5 rounded-2xl group-hover:bg-emerald-500 group-hover:text-black transition-colors">  <ImageIcon size={24}/>  </div>  <ChevronRight size={20} className="text-neutral-700 group-hover:text-white transition-colors" />  </div>  <h3 className="text-white text-xl mb-2 tracking-wider uppercase">Portfolio</h3>  <p className="text-sm text-neutral-500">Update your latest tattoo works and gallery items.</p>  </Link>   <Link href="/admin/blogs" className="bg-neutral-900 border border-white/5 p-8 rounded-[2.5rem] group hover:border-emerald-500/50 transition-all duration-500">  <div className="flex justify-between items-start mb-4">  <div className="p-3 bg-white/5 rounded-2xl group-hover:bg-emerald-500 group-hover:text-black transition-colors">  <PenTool size={24}/>  </div>  <ChevronRight size={20} className="text-neutral-700 group-hover:text-white transition-colors" />  </div>  <h3 className="text-white text-xl mb-2 tracking-wider uppercase">Journal</h3>  <p className="text-sm text-neutral-500">Write aftercare guides and studio news.</p>  </Link>  </div>   {/* SIMPLE BOOKING LIST */}  <div className="bg-neutral-950 border border-white/5 rounded-[3rem] overflow-hidden">  <div className="p-8 border-b border-white/5 flex items-center justify-between">  <h3 className="text-lg font-display text-white uppercase tracking-wider flex items-center gap-3">  <Calendar size={20} className="text-white" /> Recent Inquiries  </h3>  </div>   <div className="p-8">  {loading ? (  <div className="space-y-4">  {[1, 2].map(i => <div key={i} className="h-24 bg-white/5 animate-pulse rounded-3xl" />)}  </div>  ) : bookings.length > 0 ? (  <div className="space-y-4">  {bookings.map((booking) => (  <Link key={booking.id} href={`/admin/bookings/${booking.id}`} className="bg-neutral-900/50 border border-white/5 p-6 rounded-3xl flex flex-wrap items-center justify-between gap-6">  <div className="flex items-center gap-6">  <div className="h-12 w-12 bg-white/5 rounded-2xl flex items-center justify-center text-white ">  {booking.name.charAt(0)}  </div>  <div>  <h4 className="text-white text-lg leading-none mb-2">{booking.name}</h4>  <div className="flex flex-wrap gap-4 text-[11px] font-medium uppercase tracking-wider text-neutral-500">  <span className="flex items-center gap-1.5"><Phone size={12} className="text-white"/> {booking.contactNumber}</span>  <span className="flex items-center gap-1.5"><Clock size={12} className="text-white"/> {new Date(booking.scheduledAt).toLocaleDateString()}</span>  <span className={`px-2 py-0.5 rounded-md text-[9px] ${booking.status === 'PENDING' ? 'bg-orange-500/10 text-orange-500' : 'bg-emerald-500/10 text-white'}`}>  {booking.status}  </span>  </div>  </div>  </div>   {/* Simplified info view - No action buttons */}  <div className="hidden lg:block text-right">  <p className="text-[10px] text-neutral-600 uppercase font-black tracking-widest mb-1 text-right">Message Preview</p>  <p className="text-sm text-neutral-400 max-w-xs truncate">{booking.description || 'No details provided'}</p>  </div>  </Link>  ))}  </div>  ) : (  <div className="py-20 text-center">  <AlertCircle size={40} className="mx-auto text-neutral-800 mb-4" />  <p className="text-neutral-600 uppercase text-xs tracking-widest">No Recent Inquiries</p>  </div>  )}  </div>  </div>  </main>  </div>  ); }